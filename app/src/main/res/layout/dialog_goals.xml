<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="24dp"
        android:paddingTop="8dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/goals_hint"
            android:textColor="#888888"
            android:textSize="13sp" />

        <EditText
            android:id="@+id/goalKcal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/kcal_full_hint"
            android:inputType="number"
            android:maxLines="1" />

        <EditText
            android:id="@+id/goalProtein"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/protein_hint"
            android:inputType="number"
            android:maxLines="1" />

        <EditText
            android:id="@+id/goalFat"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/fat_hint"
            android:inputType="number"
            android:maxLines="1" />

        <EditText
            android:id="@+id/goalCarbs"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/carbs_hint"
            android:inputType="number"
            android:maxLines="1" />

    </LinearLayout>
</ScrollView>
